error codes
magic values
match and if let to unwrap_or/ unwrap_or_else
b already set
search for println
clickable vram display

put inputs outside display
track leaks
use return instead of quit
static lazy regex ~

help for param and debug
day counter carry bit

ops as lazy statics
